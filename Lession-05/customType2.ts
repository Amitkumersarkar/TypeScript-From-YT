type User = {
    userName: string;
    userId: number;
};

let users: User[] = [];

users.push({ userName: 'Amit', userId: 283 });
users.push({ userName: 'Niloy', userId: 285 });
users.push({ userName: 'Anik', userId: 288 });

console.log(users);

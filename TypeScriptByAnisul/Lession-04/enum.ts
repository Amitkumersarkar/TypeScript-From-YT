// enum-store constants; duplicate value is not allow
// enum type : numeric,string,hetergenous

// numeric enum declaration process

enum RequestType {
    readData,
    saveData,
    deleteData,
}
console.log(RequestType);